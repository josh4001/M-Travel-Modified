import React from 'react';

interface LiveVehicleTrackingVideoProps {
  vehicleModel?: string;
  plateNumber?: string;
  speed?: number;
  progress?: number;
  pickup?: string;
  destination?: string;
  driverName?: string;
}

export const LiveVehicleTrackingVideo: React.FC<LiveVehicleTrackingVideoProps> = (_props) => {
  return null;
};
