import type { Vehicle } from '@/types';

// Mock vehicles strictly emptied per platform requirements.
// Only host-registered vehicles approved by admin will appear in the system.
export const MOCK_VEHICLES: Vehicle[] = [];
